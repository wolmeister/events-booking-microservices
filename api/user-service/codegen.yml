schema: ./src/schema.graphql
generates:
  ./src/__generated__/resolvers-types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      federation: true
      scalars:
        Date: Date
